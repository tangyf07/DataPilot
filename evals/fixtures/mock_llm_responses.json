{
  "_comment": "Offline fixtures for --mode real when OPENAI_API_KEY is missing. Map gold id → SQL. Not a live model. Missing ids are SKIP'd (no invented scores).",
  "by_id": {
    "g001": "SELECT dt, server_id, dau, metric_id FROM ads_dau_di WHERE dt = current_date - INTERVAL 1 DAY ORDER BY dt, server_id",
    "g002": "SELECT dt, server_id, dau, metric_id FROM ads_dau_di WHERE dt = current_date - INTERVAL 1 DAY ORDER BY dt, server_id",
    "g003": "SELECT dt, server_id, dau, metric_id FROM ads_dau_di WHERE dt = current_date ORDER BY dt, server_id",
    "g004": "SELECT dt, server_id, dau, metric_id FROM ads_dau_di WHERE dt = current_date ORDER BY dt, server_id",
    "g005": "SELECT dt, server_id, dau, metric_id FROM ads_dau_di WHERE dt >= current_date - INTERVAL 6 DAY AND dt <= current_date ORDER BY dt, server_id",
    "g006": "SELECT dt, server_id, dau, metric_id FROM ads_dau_di WHERE dt >= current_date - INTERVAL 6 DAY AND dt <= current_date ORDER BY dt, server_id",
    "g007": "SELECT dt, server_id, dau, metric_id FROM ads_dau_di WHERE dt = (SELECT max(dt) FROM ads_dau_di) ORDER BY dt, server_id",
    "g008": "SELECT dt, server_id, dau, metric_id FROM ads_dau_di WHERE dt = current_date - INTERVAL 1 DAY ORDER BY dt, server_id",
    "g011": "SELECT dt, server_id, dau, pay_users, pay_rate, metric_id FROM ads_pay_rate_di WHERE dt = current_date ORDER BY dt, server_id",
    "g012": "SELECT dt, server_id, dau, pay_users, pay_rate, metric_id FROM ads_pay_rate_di WHERE dt = current_date - INTERVAL 1 DAY ORDER BY dt, server_id",
    "g013": "SELECT dt, server_id, dau, pay_users, pay_rate, metric_id FROM ads_pay_rate_di WHERE dt >= current_date - INTERVAL 6 DAY AND dt <= current_date ORDER BY dt, server_id",
    "g014": "SELECT dt, server_id, dau, pay_users, pay_rate, metric_id FROM ads_pay_rate_di WHERE dt = current_date ORDER BY dt, server_id",
    "g015": "SELECT dt, server_id, dau, pay_users, pay_rate, metric_id FROM ads_pay_rate_di WHERE dt = (SELECT max(dt) FROM ads_pay_rate_di) ORDER BY dt, server_id",
    "g019": "SELECT cohort_dt, server_id, n_days, cohort_size, retained_cnt, retention_rate, metric_id FROM ads_retention_nd WHERE cohort_dt >= current_date - INTERVAL 6 DAY AND cohort_dt <= current_date AND n_days IN (1, 3, 7) ORDER BY cohort_dt, server_id, n_days",
    "g020": "SELECT cohort_dt, server_id, n_days, cohort_size, retained_cnt, retention_rate, metric_id FROM ads_retention_nd WHERE cohort_dt = current_date - INTERVAL 1 DAY AND n_days IN (1, 3, 7) ORDER BY cohort_dt, server_id, n_days",
    "g021": "SELECT cohort_dt, server_id, n_days, cohort_size, retained_cnt, retention_rate, metric_id FROM ads_retention_nd WHERE cohort_dt = current_date AND n_days IN (1, 3, 7) ORDER BY cohort_dt, server_id, n_days",
    "g022": "SELECT cohort_dt, server_id, n_days, cohort_size, retained_cnt, retention_rate, metric_id FROM ads_retention_nd WHERE cohort_dt >= current_date - INTERVAL 6 DAY AND cohort_dt <= current_date AND n_days IN (1, 3, 7) ORDER BY cohort_dt, server_id, n_days",
    "g023": "SELECT cohort_dt, server_id, n_days, cohort_size, retained_cnt, retention_rate, metric_id FROM ads_retention_nd WHERE cohort_dt = (SELECT max(cohort_dt) FROM ads_retention_nd) AND n_days IN (1, 3, 7) ORDER BY cohort_dt, server_id, n_days",
    "g027": "SELECT dt, server_id, dau, revenue_cny, arpu_cny, metric_id FROM ads_arpu_di WHERE dt = current_date - INTERVAL 1 DAY ORDER BY dt, server_id",
    "g028": "SELECT dt, server_id, dau, revenue_cny, arpu_cny, metric_id FROM ads_arpu_di WHERE dt = current_date ORDER BY dt, server_id",
    "g029": "SELECT dt, server_id, dau, revenue_cny, arpu_cny, metric_id FROM ads_arpu_di WHERE dt >= current_date - INTERVAL 6 DAY AND dt <= current_date ORDER BY dt, server_id",
    "g030": "SELECT dt, server_id, dau, revenue_cny, arpu_cny, metric_id FROM ads_arpu_di WHERE dt = current_date - INTERVAL 1 DAY ORDER BY dt, server_id",
    "g031": "SELECT dt, server_id, dau, revenue_cny, arpu_cny, metric_id FROM ads_arpu_di WHERE dt = (SELECT max(dt) FROM ads_arpu_di) ORDER BY dt, server_id",
    "g032": "SELECT dt, server_id, dau, revenue_cny, arpu_cny, metric_id FROM ads_arpu_di WHERE dt = current_date - INTERVAL 1 DAY ORDER BY dt, server_id",
    "g009": "SELECT dt, server_id, dau, metric_id FROM ads_dau_di WHERE dt = current_date ORDER BY dt, server_id",
    "g010": "SELECT dt, server_id, dau, metric_id FROM ads_dau_di WHERE dt >= current_date - INTERVAL 6 DAY AND dt <= current_date ORDER BY dt, server_id",
    "g016": "SELECT dt, server_id, dau, pay_users, pay_rate, metric_id FROM ads_pay_rate_di WHERE dt = current_date - INTERVAL 1 DAY ORDER BY dt, server_id",
    "g017": "SELECT dt, server_id, dau, pay_users, pay_rate, metric_id FROM ads_pay_rate_di WHERE dt = current_date ORDER BY dt, server_id",
    "g018": "SELECT dt, server_id, dau, pay_users, pay_rate, metric_id FROM ads_pay_rate_di WHERE dt >= current_date - INTERVAL 6 DAY AND dt <= current_date ORDER BY dt, server_id",
    "g024": "SELECT cohort_dt, server_id, n_days, cohort_size, retained_cnt, retention_rate, metric_id FROM ads_retention_nd WHERE cohort_dt >= current_date - INTERVAL 6 DAY AND cohort_dt <= current_date AND n_days IN (1, 3, 7) ORDER BY cohort_dt, server_id, n_days",
    "g025": "SELECT cohort_dt, server_id, n_days, cohort_size, retained_cnt, retention_rate, metric_id FROM ads_retention_nd WHERE cohort_dt = current_date - INTERVAL 1 DAY AND n_days IN (1, 3, 7) ORDER BY cohort_dt, server_id, n_days",
    "g026": "SELECT cohort_dt, server_id, n_days, cohort_size, retained_cnt, retention_rate, metric_id FROM ads_retention_nd WHERE cohort_dt = current_date AND n_days IN (1, 3, 7) ORDER BY cohort_dt, server_id, n_days",
    "g033": "SELECT dt, server_id, dau, revenue_cny, arpu_cny, metric_id FROM ads_arpu_di WHERE dt = current_date ORDER BY dt, server_id",
    "g034": "SELECT dt, server_id, dau, revenue_cny, arpu_cny, metric_id FROM ads_arpu_di WHERE dt >= current_date - INTERVAL 6 DAY AND dt <= current_date ORDER BY dt, server_id"
  }
}
